namespace MonTilt.Driver
{
    public enum Orientation
    {
        Landscape = 0,       // 0°
        Portrait = 1,        // 90°
        LandscapeFlipped = 2, // 180°
        PortraitFlipped = 3   // 270°
    }
}